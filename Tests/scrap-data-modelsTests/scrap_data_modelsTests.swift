import XCTest
@testable import scrap_data_models

final class scrap_data_modelsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(scrap_data_models().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
